import generateChildGenes from "./generateChildGenes"

//Generates child-genes from parentgenes, TODO: update to be able to set usestate for parents
export default function crossPeas(genes1, genes2){
    generateChildGenes(genes1, genes2)
}